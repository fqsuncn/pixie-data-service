import px

# 获取所有连接的当前状态
df = px.DataFrame(table='conn_stats', start_time='-30s')
df.namespace = df.ctx['namespace']
df.pod = df.ctx['pod']

# 显示关键连接状态信息
df = df[['time_', 'pod', 'remote_addr', 'remote_port', 'conn_active', 'bytes_sent', 'bytes_recv']]
px.display(df)